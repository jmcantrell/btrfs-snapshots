#!/usr/bin/env bash

# Show that the end of options marker is recognized.

set -eu

. "$LIB_DIR"/init.sh
. "$TESTS_LIB_DIR"/init.sh

printf -v stderr "$TEXT_ACTION_INVALID\n" -h

assert_failure assert_stderr "$stderr" \
    btrfs-snapshots -- -h
