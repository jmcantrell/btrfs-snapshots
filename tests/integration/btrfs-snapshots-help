#!/usr/bin/env bash

# Show that help can be requested at the command line.

set -eu

. "$LIB_DIR"/init.sh
. "$TESTS_LIB_DIR"/init.sh

assert_success assert_stdout "$TEXT_USAGE"$'\n' \
    btrfs-snapshots -h
