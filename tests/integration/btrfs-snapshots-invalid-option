#!/usr/bin/env bash

# Show that `btrfs-snapshots` complains about invalid options.

set -euo pipefail

. "$LIB_DIR"/init.bash
. "$TESTS_LIB_DIR"/init.bash

option=-x

stderr="invalid option: $option"

assert_failure assert_stderr_match "$stderr" \
    btrfs-snapshots "$option"
